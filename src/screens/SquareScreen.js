import React from "react";
import { Text, StyleSheet, View } from "react-native";

const SquareScreen = () => {
  return <Text>Square demo</Text>;
};

const styles = StyleSheet.create({});

export default SquareScreen;
